#encoding: utf-8
When /^I view them as a "(.*?)"$/ do |view|
  pending # express the regexp above with the code you wish you had
end

Then /^I see the following output on "(.*?)":$/ do |output, string|
  pending # express the regexp above with the code you wish you had
end

#Given /^the following "([^ ]+)s" exist:$/i do |factory,table|
#  table.hashes.each do |element|
#    binding.pry
#    FactoryGirl.build(factory)
#    #attributes = FactoryGirl.attributes_for(factory.to_sym)
#  end
#end
